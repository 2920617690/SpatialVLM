inherits: "configs/base.yaml"

stage: "stage1_imitation"

dataset:
  modes:
    - "policy"

training:
  num_train_epochs: 1
  learning_rate: 1.0e-5
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 16
