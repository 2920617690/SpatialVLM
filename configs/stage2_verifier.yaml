inherits: "configs/base.yaml"

stage: "verifier"

training:
  epochs: 4
  batch_size: 32
  learning_rate: 3.0e-4

optimize:
  train_proposal: false
  train_pointer: false
  train_verifier: true
  train_decision: false
